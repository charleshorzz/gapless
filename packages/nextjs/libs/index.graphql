# Query
query postCreateds($first: Int, $skip: Int, $orderBy: Int) {
  postCreateds(first: $first, skip: $skip, orderBy: $orderBy) {
    id
    author
    postData
    owner
    blockNumber
    blockTimestamp
  }
}
